# Inventário LocalStack — EC2 simulada via container Docker
# O banco de dados é gerenciado pelo RDS (LocalStack) — sem host separado.

[api]
ec2-api ansible_host=127.0.0.1 ansible_port=2222

[all:vars]
ansible_python_interpreter=/usr/bin/python3
