# Terraform
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/.terraform.lock.hcl

# Docker build artifacts
docker/authorized_keys

# Ansible SSH keys
ansible/keys/*
!ansible/keys/.gitignore

# LocalStack data volume
localstack-data/
